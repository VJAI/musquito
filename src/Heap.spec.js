import Heap from './Heap';

describe('Heap', () => {

  let heap = null;

  beforeEach(() => {
    heap = new Heap(1);
  });

  afterEach(() => {
    heap.destroy();
  });

  describe('on adding sound', () => {

    it('should store the sound', () => {

    });
  });

  describe('on calling free', () => {

  });
});
