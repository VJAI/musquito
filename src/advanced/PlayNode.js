import BaseNode from './BaseNode';

class PlayNode extends BaseNode {

  /**
   * Executed the node's behavior.
   * @private
   */
  _execute() {
    throw new Error('Not yet implemented!');
  }
}

export { PlayNode as default };
